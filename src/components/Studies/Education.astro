---
import { education as educationEs } from "@cv";
import { education as educationEn } from "@cv-en"

const lang = Astro.currentLocale ?? 'es'
const education = lang === 'en' ? educationEn : educationEs;

const titles = lang === 'en' ? {
  education: 'Education',
} : {
  education: 'Educación'
}
---

<div class="w-full">
  <h3 class="text-xl">{titles.education}</h3>
  <ul class="flex max-lg:flex-col gap-4 xl:h-50">
    {
      education.map(({ institution, area, studyType }) => {
        return (
          <div class="container h-60 xl:h-11/12 bg-card p-4 rounded-2xl cursor-pointer hover:drop-shadow-primary-md transition-all duration-1000">
            <article>
              <header>
                <div>
                  <h3 class="max-lg:text-lg text-xl font-bold">
                    {institution}
                  </h3>
                  <h4 class="max-lg:text-sm text-xl text-gray-300">{area}</h4>
                </div>
                <h5>{studyType}</h5>
              </header>
            </article>
          </div>
        );
      })
    }
  </ul>
</div>
