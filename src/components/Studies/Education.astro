---
import { education } from "@cv";
import BoxContainer from "../BoxContainer.astro";
---

<div class="w-full">
  <h3 class="text-2xl">Educación</h3>
  <ul class="flex gap-6">
    {
      education.map(({ institution, area, studyType }) => {
        return (
          <BoxContainer>
            <article>
              <header>
                <div>
                  <h3 class="text-xl font-bold">{institution}</h3>
                  <h4 class="text-xl text-gray-300">{area}</h4>
                </div>
                <h5>{studyType}</h5>
              </header>
            </article>
          </BoxContainer>
        );
      })
    }
  </ul>
</div>
